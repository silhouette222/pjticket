<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="QNAMapper">
	<select id="selectQNAByNo" resultType="qna">
		select * from qna
		where no=#{no}	
	</select>
	
	<select id="selectQNAList" resultType="qna">
		select * from qna
		order by no desc, date desc
	</select>
	
	<update id="insertQNA">
		insert into qna (qna_no, qna_title, qna_content, qna_pw, qna_pwin, qna_reply, am_id, mem_id)
		values(qna_seq.nextval, #{qna_title}, #{qna_content}, #{qna_pw}, #{qna_pwin}, #{qna_reply}, #{am_id}, #{mem_id})
	</update>
	
	<update id="updateQNA">
		update qna set qna_title=#{qna_title}, qna_content=#{qna_content}, qna_pw=#{qna_pw}, qna_pwin=#{qna_pwin}, qna_reply=#{qna_reply}, am_id=#{am_id}, mem_id=#{mem_id}
		where qna_no = #{qna_no}
	</update>
	
	<update id="deleteQNA">
		delete from qna where qna_no=#{qna_no}
	</update>
	
	<select id="selectCountPaging" resultType="int">
		select count(no) from qna
	</select>
	
	<sql id="search">
		<if test="searchType != null">
			<if test="searchType == 't'.toString()">
				and qna_title like '%'||#{keyword}||'%'
			</if>
			<if test="searchType == 'c'.toString()">
				and qna_content like '%'||#{keyword}||'%'
			</if>
			<if test="searchType == 'w'.toString()">
				and mem_id like '%'||#{keyword}||'%'
			</if>
			<if test="searchType == 'tc'.toString()">
				and( qna_title like '%'||#{keyword}||'%' OR
				qna_content like '%'||#{keyword}||'%')
			</if>
			<if test="searchType == 'tw'.toString()">
				and( qna_title like '%'||#{keyword}||'%' OR
				mem_id like '%'||#{keyword}||'%')
			</if>
			<if test="searchType == 'tcw'.toString()">
				and( qna_title like '%'||#{keyword}||'%' OR
				qna_content like '%'||#{keyword}||'%' OR
				mem_id like '%'||#{keyword}||'%')
			</if>
		</if>
	</sql>
	
	<select id="selectSearchQNAList" resultType="qna">
		select * from qna
		<include refid="search"></include>
		order by qna_no desc
	</select>
	
	<select id="selectSearchQNACount" resultType="int">
		select count(qna_no) from qna
		<include refid="search"></include>
	</select>
	
	<select id="selectMaxNo" resultType="int">
		select max(qna_no) from qna
	</select>
	
</mapper>